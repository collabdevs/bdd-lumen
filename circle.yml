general:
  branches:
    only:
      - master
machine:
  timezone:
    America/New_York
  environment:
    CIRCLE_ENV: test
#dependencies:
#  pre:
    # touch so pra testar
    #- touch build.log && echo 'logando' > 

test:
  override:
    - ./build.sh  --project lumen-bdd;
    #- ./codeception-ci-generator.sh --project lumen-bdd;
    #- ./codeception-ci-generator.sh --testing 3;
